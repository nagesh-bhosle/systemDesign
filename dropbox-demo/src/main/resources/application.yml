# ============================================================
# Spring Boot configuration for Dropbox Demo
# ============================================================

# --- Server ---
server:
  port: 8080
  tomcat:
    max-swallow-size: -1          # Don't limit request body swallowing
    max-connections: 100
    threads:
      max: 20

# --- Spring multipart config (critical for large files) ---
spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 0       # Write to disk immediately (streaming)
      max-file-size: 10GB          # Max single file size
      max-request-size: 10GB       # Max total request size
      location: ${java.io.tmpdir}  # Temp directory for large file staging

  application:
    name: dropbox-demo

  # --- H2 Database (Dropbox metadata) ---
  datasource:
    url: jdbc:h2:mem:dropbox;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

# --- Azure Blob Storage (Azurite) ---
azure:
  blob:
    # Azurite default connection string
    connection-string: "DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://127.0.0.1:10000/devstoreaccount1;"
    container-name: "uploads"
    # Block size for large file uploads (in bytes). 8MB is optimal for large files.
    block-size-bytes: 8388608
    # Max concurrency for parallel block uploads
    max-concurrency: 8

# --- Dropbox-specific config ---
dropbox:
  chunk-size-bytes: 4194304    # 4 MB per chunk (client should match this)

# --- Logging ---
logging:
  level:
    com.example.blobupload: DEBUG
    com.azure: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"